'use client';

// The actual application form lives at /[companyPublicUrl]/[listingId]
// This component just provides a client-side "Apply Now" scroll anchor
// and could be extended later with an inline application form

export default function JobApplySection() {
  return null;
}
